# MongoDB（若 URI 已含库名，如 .../openclaw_llm_bridge?authSource=admin，可省略 MONGODB_DB）
MONGODB_URI=mongodb://localhost:27017
# MONGODB_DB=openclaw_llm_bridge

# 管理端鉴权（必填）
ADMIN_TOKEN=your-admin-secret-token

# LiteLLM / Azure 后端
LLM_API_KEY=
LLM_MODEL=gpt-5
LLM_ENDPOINT=https://azure-openai-api.openclaw.cn
LLM_API_VERSION=2024-12-01-preview

# 可选
# TIKTOKEN_ENCODING=cl100k_base
